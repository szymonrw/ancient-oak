#!/bin/bash

source bin/env

mkdir -p "$DIST"

browserify "$INDEX" --standalone I -o "$STANDALONE"
uglifyjs -m -c --source-map "$MAP" "$STANDALONE" > "$MINIFIED"
bin/bower-config > bower.json

cp "$STANDALONE" "$npm_package_name.js"
cp "$MINIFIED" "$npm_package_name.min.js"
