#!/bin/bash

source bin/env

mkdir -p "$DIST"

node_modules/browserify/bin/cmd.js "$INDEX" --standalone I -o "$STANDALONE"
node_modules/uglify-js/bin/uglifyjs -m -c --source-map "$MAP" "$STANDALONE" > "$MINIFIED"
